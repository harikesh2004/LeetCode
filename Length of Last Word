class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        s=s.strip()
        a=s.split(" ")
        for i in range (1,len(a)+1):
            if isinstance(a[-i], str):
                return len(a[-i])


        
